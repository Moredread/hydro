#ifndef __CELL_HPP
#define __CELL_HPP

struct Cell {

    double foo;
    double bar;

    Cell(double foo = 0.0, double bar = 0.0) {
        this->foo = foo;
        this->bar = bar;
    }

};

#endif

